<%= form_with(model: ticket_list, local: true) do |form| %>
  <% if ticket_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_list.errors.count, "error") %> prohibited this ticket_list from being saved:</h2>

      <ul>
        <% ticket_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
